mod arguments;
// mod config;

fn main() {
    let args = arguments::get_args();
    // println!("{:?}", args);


    // let config = config::get_config();

    // println!("{:#?}", config);
    // config::create_config();
}